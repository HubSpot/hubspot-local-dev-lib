import { ENVIRONMENTS as __ENVIRONMENTS } from './environments';
import { ALLOWED_EXTENSIONS as __ALLOWED_EXTENSIONS } from './extensions';

export const ENVIRONMENTS = __ENVIRONMENTS;
export const ALLOWED_EXTENSIONS = __ALLOWED_EXTENSIONS;
